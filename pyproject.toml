[project]
name = "krishi-sahayak"
version = "0.1.0"
description = "Flask ML crop recommendation app"
requires-python = ">=3.10"

dependencies = [
    "flask",
    "numpy",
    "pandas",
    "scikit-learn",
    "joblib",
    "requests"
]
